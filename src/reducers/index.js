import React from 'react';

//  const rootReducer = (state={name:'kalpana'},action) => {
//   return state;
// };

// export default rootReducer;

export default (state={name:"kalpana"},action)=>{
    return state;
}